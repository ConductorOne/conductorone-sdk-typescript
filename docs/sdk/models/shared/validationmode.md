# ValidationMode

Validation approach. See MicrosoftValidationMode for details on each mode.

## Example Usage

```typescript
import { ValidationMode } from "conductorone-sdk-typescript/sdk/models/shared";

let value: ValidationMode = "MICROSOFT_VALIDATION_MODE_UNSPECIFIED";

// Open enum: unrecognized values are captured as Unrecognized<string>
```

## Values

```typescript
"MICROSOFT_VALIDATION_MODE_UNSPECIFIED" | "MICROSOFT_VALIDATION_MODE_ACRS" | "MICROSOFT_VALIDATION_MODE_OIDC" | Unrecognized<string>
```