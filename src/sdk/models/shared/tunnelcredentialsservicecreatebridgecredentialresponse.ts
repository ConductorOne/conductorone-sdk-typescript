/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  TunnelCredential,
  TunnelCredential$inboundSchema,
} from "./tunnelcredential.js";

/**
 * The TunnelCredentialsServiceCreateBridgeCredentialResponse message.
 */
export type TunnelCredentialsServiceCreateBridgeCredentialResponse = {
  /**
   * TunnelCredential is the API view of one OAuth credential within a bridge.
   *
   * @remarks
   *  The plaintext client_secret is only populated on the CreateBridgeCredential
   *  response.
   */
  tunnelCredential?: TunnelCredential | undefined;
};

/** @internal */
export const TunnelCredentialsServiceCreateBridgeCredentialResponse$inboundSchema:
  z.ZodType<
    TunnelCredentialsServiceCreateBridgeCredentialResponse,
    z.ZodTypeDef,
    unknown
  > = z.object({
    credential: TunnelCredential$inboundSchema.optional(),
  }).transform((v) => {
    return remap$(v, {
      "credential": "tunnelCredential",
    });
  });

export function tunnelCredentialsServiceCreateBridgeCredentialResponseFromJSON(
  jsonString: string,
): SafeParseResult<
  TunnelCredentialsServiceCreateBridgeCredentialResponse,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      TunnelCredentialsServiceCreateBridgeCredentialResponse$inboundSchema
        .parse(JSON.parse(x)),
    `Failed to parse 'TunnelCredentialsServiceCreateBridgeCredentialResponse' from JSON`,
  );
}
