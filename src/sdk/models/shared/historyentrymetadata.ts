/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { HistoryActor, HistoryActor$inboundSchema } from "./historyactor.js";
import {
  HistoryAnnotation,
  HistoryAnnotation$inboundSchema,
} from "./historyannotation.js";

/**
 * Storage-model enum re-exported here for wire compatibility with the
 *
 * @remarks
 *  storage row. UNSPECIFIED should never appear on the wire.
 */
export const ChangeKind = {
  ChangeKindUnspecified: "CHANGE_KIND_UNSPECIFIED",
  ChangeKindCreate: "CHANGE_KIND_CREATE",
  ChangeKindPut: "CHANGE_KIND_PUT",
  ChangeKindHardDelete: "CHANGE_KIND_HARD_DELETE",
} as const;
/**
 * Storage-model enum re-exported here for wire compatibility with the
 *
 * @remarks
 *  storage row. UNSPECIFIED should never appear on the wire.
 */
export type ChangeKind = OpenEnum<typeof ChangeKind>;

/**
 * HistoryEntryMetadata is the shared metadata envelope embedded on every
 *
 * @remarks
 *  per-service HistoryEntry. The strongly-typed snapshot lives on the
 *  per-service entry message alongside this envelope.
 */
export type HistoryEntryMetadata = {
  /**
   * HistoryActor is a typed reference to whoever performed the change.
   *
   * @remarks
   *  kind mirrors the storage-model ActorKind enum; user_id is set when
   *  kind corresponds to a user principal (API / SUPPORT) so the frontend
   *  can resolve the user via its own avatar / lookup hooks. Protos
   *  reference objects by id; the frontend renders / caches itself.
   *
   *  The raw passport fields (token_id / principal_id) intentionally do not
   *  leave the server. Non-user actors (workflow, connector, internal) are
   *  identified by `kind` alone; correlating IDs (workflow_run_id, etc.)
   *  flow through `HistoryAnnotation` instead of being plucked into the
   *  actor message.
   */
  historyActor?: HistoryActor | undefined;
  /**
   * Server-rendered annotations: known keys carry display_label and
   *
   * @remarks
   *  (for ticket_id, etc.) display_url resolved from tenant config.
   *  Cap mirrors the per-object annotation ceiling (16).
   */
  annotations?: Array<HistoryAnnotation> | null | undefined;
  /**
   * Storage-model enum re-exported here for wire compatibility with the
   *
   * @remarks
   *  storage row. UNSPECIFIED should never appear on the wire.
   */
  changeKind?: ChangeKind | undefined;
  createdAt?: Date | undefined;
  /**
   * KSUID. Same value as c1.models.history.v1.ObjectHistory.id.
   */
  id?: string | undefined;
  /**
   * System Log event id — KSUID of the OCSF event recorded for this
   *
   * @remarks
   *  write. Empty for non-RPC writes (workflows, cron). Customer-facing
   *  copy says "System Log event"; the underlying format is OCSF.
   */
  syslogEventId?: string | undefined;
  /**
   * OTel trace correlation. Empty when no valid span at write time.
   *
   * @remarks
   *  32-hex-char otel trace id or empty.
   */
  traceId?: string | undefined;
};

/** @internal */
export const ChangeKind$inboundSchema: z.ZodType<
  ChangeKind,
  z.ZodTypeDef,
  unknown
> = openEnums.inboundSchema(ChangeKind);

/** @internal */
export const HistoryEntryMetadata$inboundSchema: z.ZodType<
  HistoryEntryMetadata,
  z.ZodTypeDef,
  unknown
> = z.object({
  actor: HistoryActor$inboundSchema.optional(),
  annotations: z.nullable(z.array(HistoryAnnotation$inboundSchema)).optional(),
  changeKind: ChangeKind$inboundSchema.optional(),
  createdAt: z.string().datetime({ offset: true }).transform(v => new Date(v))
    .optional(),
  id: z.string().optional(),
  syslogEventId: z.string().optional(),
  traceId: z.string().optional(),
}).transform((v) => {
  return remap$(v, {
    "actor": "historyActor",
  });
});

export function historyEntryMetadataFromJSON(
  jsonString: string,
): SafeParseResult<HistoryEntryMetadata, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => HistoryEntryMetadata$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'HistoryEntryMetadata' from JSON`,
  );
}
