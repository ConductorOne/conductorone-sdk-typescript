/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The TaskAuditPendingResult message.
 */
export type TaskAuditPendingResult = {
  /**
   * Human-readable explanation of why the action is pending. Rendered in the
   *
   * @remarks
   *  ticket audit log so admins can see what the action is waiting on (e.g.,
   *  "GitHub org invite sent. User must accept the invitation before team
   *  membership can be granted."). Naming mirrors TaskAuditErrorResult.error_reason
   *  and TaskAuditCancelledResult.cancel_reason for consistency.
   */
  pendingReason?: string | undefined;
};

/** @internal */
export const TaskAuditPendingResult$inboundSchema: z.ZodType<
  TaskAuditPendingResult,
  z.ZodTypeDef,
  unknown
> = z.object({
  pendingReason: z.string().optional(),
});

export function taskAuditPendingResultFromJSON(
  jsonString: string,
): SafeParseResult<TaskAuditPendingResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => TaskAuditPendingResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'TaskAuditPendingResult' from JSON`,
  );
}
