/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * AnnouncedTunnelService is one service entry the appliance declared in its
 *
 * @remarks
 *  wormhole HELLO frame. Read live from the discovery store; not persisted.
 */
export type AnnouncedTunnelService = {
  /**
   * Logical name of the service as declared by the appliance.
   */
  name?: string | undefined;
  /**
   * TCP port the service listens on inside the appliance network.
   */
  port?: number | undefined;
  /**
   * Optional URL path prefix for the service.
   */
  servicePath?: string | undefined;
  /**
   * Application-level protocol type (e.g. "http", "grpc").
   */
  serviceType?: string | undefined;
  /**
   * Transport protocol used by the wormhole tunnel (e.g. "tcp").
   */
  transportType?: string | undefined;
};

/** @internal */
export const AnnouncedTunnelService$inboundSchema: z.ZodType<
  AnnouncedTunnelService,
  z.ZodTypeDef,
  unknown
> = z.object({
  name: z.string().optional(),
  port: z.number().int().optional(),
  servicePath: z.string().optional(),
  serviceType: z.string().optional(),
  transportType: z.string().optional(),
});

export function announcedTunnelServiceFromJSON(
  jsonString: string,
): SafeParseResult<AnnouncedTunnelService, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => AnnouncedTunnelService$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'AnnouncedTunnelService' from JSON`,
  );
}
