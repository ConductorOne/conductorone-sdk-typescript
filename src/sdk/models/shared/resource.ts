/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { ImportIDShape, ImportIDShape$inboundSchema } from "./importidshape.js";

/**
 * Resource-specific schema metadata.
 */
export type Resource = {
  /**
   * ImportIDShape describes the structure of the `id` value in a
   *
   * @remarks
   *  Terraform `import { to = ..., id = "..." }` block. Most resources use
   *  a single string; binding-style resources (App_Owner,
   *  App_Entitlement_Owner, …) use a composite of multiple field values.
   *
   * This message contains a oneof named shape. Only a single field of the following list may be set at a time:
   *   - singleString
   *   - composite
   */
  importIDShape?: ImportIDShape | undefined;
};

/** @internal */
export const Resource$inboundSchema: z.ZodType<
  Resource,
  z.ZodTypeDef,
  unknown
> = z.object({
  importId: ImportIDShape$inboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    "importId": "importIDShape",
  });
});

export function resourceFromJSON(
  jsonString: string,
): SafeParseResult<Resource, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Resource$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Resource' from JSON`,
  );
}
