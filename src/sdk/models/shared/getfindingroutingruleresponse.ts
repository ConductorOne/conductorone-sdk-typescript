/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  FindingRoutingRule,
  FindingRoutingRule$inboundSchema,
} from "./findingroutingrule.js";

/**
 * The GetFindingRoutingRuleResponse message.
 */
export type GetFindingRoutingRuleResponse = {
  /**
   * FindingRoutingRule configures what happens when a finding is detected.
   *
   * @remarks
   *  Rules are evaluated in two passes: tenant-wide rules first (global override),
   *  then app-scoped rules (local customization). First match wins within each pass.
   *
   *  Matching uses CEL expressions evaluated against the Finding object.
   *  Example conditions:
   *    finding.severity == FindingSeverity.CRITICAL
   *    finding.custom_tags["env"] == "production"
   *    finding.risk_score > 75 && finding.recurrence_count >= 3
   */
  findingRoutingRule?: FindingRoutingRule | undefined;
};

/** @internal */
export const GetFindingRoutingRuleResponse$inboundSchema: z.ZodType<
  GetFindingRoutingRuleResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  routingRule: FindingRoutingRule$inboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    "routingRule": "findingRoutingRule",
  });
});

export function getFindingRoutingRuleResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetFindingRoutingRuleResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetFindingRoutingRuleResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetFindingRoutingRuleResponse' from JSON`,
  );
}
