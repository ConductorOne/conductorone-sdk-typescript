/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * TaskAuditCreatedReplacementExtensionGrantTask is used when a replacement extension grant task is created
 *
 * @remarks
 *  (e.g. when an extension grant task is cancelled due to app user deletion).
 */
export type TaskAuditCreatedReplacementExtensionGrantTask = {
  /**
   * The ID of the newly created replacement task
   */
  newTaskId?: string | undefined;
  /**
   * The numeric ID of the newly created replacement task (for display)
   */
  newTaskNumericId?: number | undefined;
};

/** @internal */
export const TaskAuditCreatedReplacementExtensionGrantTask$inboundSchema:
  z.ZodType<
    TaskAuditCreatedReplacementExtensionGrantTask,
    z.ZodTypeDef,
    unknown
  > = z.object({
    newTaskId: z.string().optional(),
    newTaskNumericId: z.string().transform(v => parseInt(v, 10)).optional(),
  });

export function taskAuditCreatedReplacementExtensionGrantTaskFromJSON(
  jsonString: string,
): SafeParseResult<
  TaskAuditCreatedReplacementExtensionGrantTask,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      TaskAuditCreatedReplacementExtensionGrantTask$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'TaskAuditCreatedReplacementExtensionGrantTask' from JSON`,
  );
}
