/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Automation, Automation$inboundSchema } from "./automation.js";

/**
 * The CreateAutomationResponse message.
 */
export type CreateAutomationResponse1 = {
  automation?: Automation | null | undefined;
  /**
   * One-time absolute webhook URL for capability URL authentication, shown once at creation time.
   *
   * @remarks
   *  Contains the full URL including the embedded token (e.g. https://tenant.conductorone.com/api/v1/webhooks/incoming/{id}/t/{token}).
   *  Populated only when the webhook trigger uses capability URL authentication.
   */
  webhookCapabilityUrl?: string | undefined;
  /**
   * One-time HMAC shared secret, shown once at creation time.
   *
   * @remarks
   *  Populated only when the webhook trigger uses HMAC authentication.
   */
  webhookHmacSecret?: string | null | undefined;
};

/** @internal */
export const CreateAutomationResponse1$inboundSchema: z.ZodType<
  CreateAutomationResponse1,
  z.ZodTypeDef,
  unknown
> = z.object({
  automation: z.nullable(Automation$inboundSchema).optional(),
  webhookCapabilityUrl: z.string().optional(),
  webhookHmacSecret: z.nullable(z.string()).optional(),
});

export function createAutomationResponse1FromJSON(
  jsonString: string,
): SafeParseResult<CreateAutomationResponse1, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CreateAutomationResponse1$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CreateAutomationResponse1' from JSON`,
  );
}
