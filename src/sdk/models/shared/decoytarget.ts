/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * DecoyTarget points at the planted decoy that produced this finding.
 *
 * @remarks
 *  Populated for findings whose subject is the decoy artifact itself
 *  (e.g. decoy_credential_used), giving the UI and routing rules a
 *  uniform handle to the decoy alongside the finding_type payload.
 */
export type DecoyTarget = {
  /**
   * The decoyId field.
   */
  decoyId?: string | undefined;
};

/** @internal */
export const DecoyTarget$inboundSchema: z.ZodType<
  DecoyTarget,
  z.ZodTypeDef,
  unknown
> = z.object({
  decoyId: z.string().optional(),
});

export function decoyTargetFromJSON(
  jsonString: string,
): SafeParseResult<DecoyTarget, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DecoyTarget$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DecoyTarget' from JSON`,
  );
}
