/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import * as openEnums from "../../types/enums.js";
import { OpenEnum } from "../../types/enums.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * The kind field.
 */
export const Kind = {
  ActorKindUnspecified: "ACTOR_KIND_UNSPECIFIED",
  ActorKindApi: "ACTOR_KIND_API",
  ActorKindSlack: "ACTOR_KIND_SLACK",
  ActorKindMsteams: "ACTOR_KIND_MSTEAMS",
  ActorKindJiraCloud: "ACTOR_KIND_JIRA_CLOUD",
  ActorKindInternal: "ACTOR_KIND_INTERNAL",
  ActorKindSupport: "ACTOR_KIND_SUPPORT",
  ActorKindWorkflow: "ACTOR_KIND_WORKFLOW",
} as const;
/**
 * The kind field.
 */
export type Kind = OpenEnum<typeof Kind>;

/**
 * HistoryActor is a typed reference to whoever performed the change.
 *
 * @remarks
 *  kind mirrors the storage-model ActorKind enum; user_id is set when
 *  kind corresponds to a user principal (API / SUPPORT) so the frontend
 *  can resolve the user via its own avatar / lookup hooks. Protos
 *  reference objects by id; the frontend renders / caches itself.
 *
 *  The raw passport fields (token_id / principal_id) intentionally do not
 *  leave the server. Non-user actors (workflow, connector, internal) are
 *  identified by `kind` alone; correlating IDs (workflow_run_id, etc.)
 *  flow through `HistoryAnnotation` instead of being plucked into the
 *  actor message.
 */
export type HistoryActor = {
  /**
   * The kind field.
   */
  kind?: Kind | undefined;
  /**
   * Bare KSUID. Set when kind = ACTOR_KIND_API or ACTOR_KIND_SUPPORT.
   *
   * @remarks
   *  Empty otherwise. The frontend resolves user_id → display name via
   *  the same lookup paths it uses elsewhere (avatars, mentions, ...).
   */
  userId?: string | undefined;
};

/** @internal */
export const Kind$inboundSchema: z.ZodType<Kind, z.ZodTypeDef, unknown> =
  openEnums.inboundSchema(Kind);

/** @internal */
export const HistoryActor$inboundSchema: z.ZodType<
  HistoryActor,
  z.ZodTypeDef,
  unknown
> = z.object({
  kind: Kind$inboundSchema.optional(),
  userId: z.string().optional(),
});

export function historyActorFromJSON(
  jsonString: string,
): SafeParseResult<HistoryActor, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => HistoryActor$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'HistoryActor' from JSON`,
  );
}
