/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  HistoryEntryMetadata,
  HistoryEntryMetadata$inboundSchema,
} from "./historyentrymetadata.js";
import { MCPTool, MCPTool$inboundSchema } from "./mcptool.js";

/**
 * MCPToolHistoryEntry is one version of an MCP tool and its history metadata.
 */
export type MCPToolHistoryEntry = {
  /**
   * HistoryEntryMetadata is the shared metadata envelope embedded on every
   *
   * @remarks
   *  per-service HistoryEntry. The strongly-typed snapshot lives on the
   *  per-service entry message alongside this envelope.
   */
  historyEntryMetadata?: HistoryEntryMetadata | undefined;
  /**
   * MCPTool represents metadata about individual tools discovered from an MCP server.
   */
  mcpTool?: MCPTool | undefined;
};

/** @internal */
export const MCPToolHistoryEntry$inboundSchema: z.ZodType<
  MCPToolHistoryEntry,
  z.ZodTypeDef,
  unknown
> = z.object({
  metadata: HistoryEntryMetadata$inboundSchema.optional(),
  snapshot: MCPTool$inboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    "metadata": "historyEntryMetadata",
    "snapshot": "mcpTool",
  });
});

export function mcpToolHistoryEntryFromJSON(
  jsonString: string,
): SafeParseResult<MCPToolHistoryEntry, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => MCPToolHistoryEntry$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'MCPToolHistoryEntry' from JSON`,
  );
}
