/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../../../lib/primitives.js";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import {
  TFSchemaMapping,
  TFSchemaMapping$inboundSchema,
} from "./tfschemamapping.js";

/**
 * The GetSchemaResponse message.
 */
export type GetSchemaResponse = {
  /**
   * TFSchemaMapping describes how to translate one C1 API object into a
   *
   * @remarks
   *  single Terraform block. Variant-specific metadata (e.g. `import_id` for
   *  resources) lives on the `block` oneof.
   *
   * This message contains a oneof named block. Only a single field of the following list may be set at a time:
   *   - resource
   *   - dataSource
   */
  tfSchemaMapping?: TFSchemaMapping | undefined;
};

/** @internal */
export const GetSchemaResponse$inboundSchema: z.ZodType<
  GetSchemaResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  schema: TFSchemaMapping$inboundSchema.optional(),
}).transform((v) => {
  return remap$(v, {
    "schema": "tfSchemaMapping",
  });
});

export function getSchemaResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetSchemaResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetSchemaResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetSchemaResponse' from JSON`,
  );
}
