/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";
import { Composite, Composite$inboundSchema } from "./composite.js";
import { SingleString, SingleString$inboundSchema } from "./singlestring.js";

/**
 * ImportIDShape describes the structure of the `id` value in a
 *
 * @remarks
 *  Terraform `import { to = ..., id = "..." }` block. Most resources use
 *  a single string; binding-style resources (App_Owner,
 *  App_Entitlement_Owner, …) use a composite of multiple field values.
 *
 * This message contains a oneof named shape. Only a single field of the following list may be set at a time:
 *   - singleString
 *   - composite
 */
export type ImportIDShape = {
  /**
   * Composite import IDs combine values from multiple component fields
   *
   * @remarks
   *  per the declared `format`.
   */
  composite?: Composite | null | undefined;
  /**
   * Single-string import IDs use the resource's id value verbatim.
   */
  singleString?: SingleString | null | undefined;
};

/** @internal */
export const ImportIDShape$inboundSchema: z.ZodType<
  ImportIDShape,
  z.ZodTypeDef,
  unknown
> = z.object({
  composite: z.nullable(Composite$inboundSchema).optional(),
  singleString: z.nullable(SingleString$inboundSchema).optional(),
});

export function importIDShapeFromJSON(
  jsonString: string,
): SafeParseResult<ImportIDShape, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => ImportIDShape$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'ImportIDShape' from JSON`,
  );
}
