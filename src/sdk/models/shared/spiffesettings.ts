/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * SPIFFESettings is the kind-specific configuration block for SPIFFE
 *
 * @remarks
 *  trust-domain providers (issuer_url = spiffe://<trust-domain>).
 */
export type SPIFFESettings = {
  /**
   * HTTPS URL of the JWKS endpoint serving the trust domain's signing keys.
   *
   * @remarks
   *  Required: the spiffe:// scheme has no discovery mechanism.
   *  Typically the SPIRE OIDC Discovery Provider's /keys endpoint.
   *
   *  Mutable: updates re-validate the new URL by fetching its JWKS before
   *  persisting; the issuer (trust domain) itself remains immutable.
   */
  bundleEndpointUrl?: string | undefined;
};

/** @internal */
export const SPIFFESettings$inboundSchema: z.ZodType<
  SPIFFESettings,
  z.ZodTypeDef,
  unknown
> = z.object({
  bundleEndpointUrl: z.string().optional(),
});
/** @internal */
export type SPIFFESettings$Outbound = {
  bundleEndpointUrl?: string | undefined;
};

/** @internal */
export const SPIFFESettings$outboundSchema: z.ZodType<
  SPIFFESettings$Outbound,
  z.ZodTypeDef,
  SPIFFESettings
> = z.object({
  bundleEndpointUrl: z.string().optional(),
});

export function spiffeSettingsToJSON(spiffeSettings: SPIFFESettings): string {
  return JSON.stringify(SPIFFESettings$outboundSchema.parse(spiffeSettings));
}
export function spiffeSettingsFromJSON(
  jsonString: string,
): SafeParseResult<SPIFFESettings, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SPIFFESettings$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SPIFFESettings' from JSON`,
  );
}
