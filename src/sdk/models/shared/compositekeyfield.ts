/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../../../lib/schemas.js";
import { Result as SafeParseResult } from "../../types/fp.js";
import { SDKValidationError } from "../errors/sdkvalidationerror.js";

/**
 * CompositeKeyField names one sibling component of a composite-key
 *
 * @remarks
 *  reference lookup.
 */
export type CompositeKeyField = {
  /**
   * C1 API field name on the parent message (snake_case proto
   *
   * @remarks
   *  field). The collector reads the runtime value at this path.
   */
  c1Field?: string | undefined;
  /**
   * Attribute name in the Terraform data source's `refs[]` struct.
   *
   * @remarks
   *  Usually identical to c1_field (the conductorone provider
   *  matches them 1:1 today). Distinct fields anyway so a future
   *  provider rename is wire-safe — no migration needed.
   */
  tfField?: string | undefined;
};

/** @internal */
export const CompositeKeyField$inboundSchema: z.ZodType<
  CompositeKeyField,
  z.ZodTypeDef,
  unknown
> = z.object({
  c1Field: z.string().optional(),
  tfField: z.string().optional(),
});

export function compositeKeyFieldFromJSON(
  jsonString: string,
): SafeParseResult<CompositeKeyField, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => CompositeKeyField$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'CompositeKeyField' from JSON`,
  );
}
